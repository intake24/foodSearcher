{
  "type": "module",
  "scripts": {
    "start": "concurrently -n api,app -c green,blue \"pnpm api\" \"pnpm app\"",
    "db:embed": "pnpm --filter foodsearcher-api embed",
    "api": "pnpm --filter foodsearcher-api api",
    "app": "pnpm --filter foodsearcher-client dev",
    "clean": "pnpm exec rimraf node_modules **/node_modules **/dist dist"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.7.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/pg": "^8.15.5",
    "concurrently": "^9.0.1",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.4"
  }
}